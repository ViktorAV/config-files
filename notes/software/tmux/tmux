# Tmux терминальный мультикомпрессор
Позволяет запускать в одном терминале несколько
терминальных сессий, оставлять их работать в фоне
и работать с SSH

## Управление сессиями
Синтаксис `tmux command option`

Чтобы создать сессию можно запустить __tmux__ без параметров

- attach-session


